<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @meeting.name %>
</p>

<p>
  <strong>User:</strong>
  <%= @meeting.user %>
</p>

<p>
  <strong>Room:</strong>
  <%= @meeting.room %>
</p>

<%= link_to 'Edit', edit_room_meeting_path(params[:room_id], @meeting) %> |
<%= link_to 'Back', room_meetings_path(params[:room_id]) %>
