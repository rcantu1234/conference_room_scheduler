
<h1>Listing Meetings</h1>

<table class='table table-striped'>
  <thead>
    <tr>
      <th>Name</th>
      <th>Start Time</th>
      <th>End Time</th>
      <th>Created At</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @meetings.each do |meeting| %>
      <tr>
        <td><%= meeting.name %></td>
        <td><%= meeting.start_time %></td>
        <td><%= meeting.end_time %></td>
        <td><%= meeting.created_at %></td>
        <td><%= User.find_by(id: meeting.user_id) %></td>
        <td><%= Room.find_by(id: meeting.room_id) %></td>
        <td><%= link_to 'Show', room_meeting_path(params[:room_id], meeting) %></td>
        <td><%= link_to 'Edit', edit_room_meeting_path(params[:room_id], meeting) %></td>
        <td><%= link_to 'Destroy', "#", method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Meeting', new_room_meeting_path(params[:room_id]) %>
